-- vim-with-me is now provided locally at lua/vim-with-me (text protocol,
-- matching the gameoflife Go server built against vim-with-me v0.0.3).
-- It is loaded via Neovim's runtimepath, not lazy, so lazy must not
-- re-clone the remote (which would pull the incompatible master client).
return {}
